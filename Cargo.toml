[package]
name = "netid"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "netid"
path = "src/main.rs"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
regex = "1"

[profile.release]
strip = true
lto = true
opt-level = "z"
